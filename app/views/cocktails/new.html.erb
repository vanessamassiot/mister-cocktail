<h1>Add a new Cocktail Del Arte</h1>

<%= form_for @cocktail do |f| %>
  <%= f.text_field :name %>
  <%= f.label :picture %>
  <%= f.file_field :picture %>
  <%= f.submit "enregistrer", {class: "btn btn-danger"}  %>
<% end %>

